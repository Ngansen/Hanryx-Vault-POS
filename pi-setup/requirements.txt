flask>=3.0.0
flask-compress>=1.14
gunicorn>=21.2.0
gevent>=24.2.1
psycopg2-binary>=2.9.9
cachetools>=5.3.0
requests>=2.31.0
qrcode[pil]>=7.4.2
Pillow>=10.0.0
openai>=1.30.0

# QR scan hub — reads USB + Bluetooth HID scanners as keyboard-style input devices
evdev>=1.6.1

# Receipt printer (optional — server uses raw ESC/POS bytes by default)
# python-escpos>=3.0  # uncomment if you want the higher-level API
